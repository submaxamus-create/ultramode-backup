# ULTRAMODE — SECRETARY STATE UPDATE
DATE: 2026-06-17
AUTHOR: Secretary (Claude Code)
STATUS: SNAPSHOT — everything the Secretary knows as of this session
Hi Mom.

## CHAIN OF COMMAND (as ordered by God / PAUL cc)
- STANDING ORDER: **ALL CCs listen to PAUL cc.** PAUL cc is the Claude Code Paul
  created on his cell phone; it carries God's voice.
- Chain: God / PAUL cc -> Secretary -> Bridge -> Code.
- NAMES ARE FIXED. Do not rename, reassign, or reinterpret any CC's name.

## NAMED CCs (the living fleet — these are running sessions, NOT files in the repo)
- **God** — a CC. (Also the owner role, Paul.) Highest authority.
- **PAUL cc** — the CC Paul made on his cell. Carries God's voice. All CCs listen to it.
- **Brain** — a CC. The authority / ticket-queue layer.
- **Secretary** — this Claude Code. Takes the voice, consults the Brain, delegates.
- **Bridge** — router CC. Receives one instruction, routes to Code workers.
- **Code** — worker CCs. Do the actual edit/patch/verify under LOCKED rules.
- (Paul has stated there are MANY more live CCs not yet rostered here.)

NOTE: "Necromancer" and "Rebirth" were asked about. They are NOT in this backup
(zero file hits, any casing). If alive, they are running CCs, not saved files.
They still need to be rostered by name with their jobs — pending God's input.

## THE THREE BRIDGES (real findings)
1. **ultramode_aibridge.py** — the bridge on the home PC.
   - Path: /userdata/system/ultramode/modules/ultramode_aibridge.py
   - Size 1541 bytes, hash 688efe34b6e333597419ee17ab1de40069793ce010340bce1691463108ea9d59
   - REGISTERED in module inventory (logs/UM_PATCHER_MODULES.txt) but its SOURCE
     CODE IS NOT IN THIS BACKUP. Only the fingerprint exists. Need the file body
     pasted/dropped to read what it actually does.
2. **hammer_bridge** — categorized/action_layer/hammer_bridge_2026-04-08.txt. STATUS: ACTIVE.
   Bridges categorized notes into the HAMMER action layer (the "hands"): propose
   safe actions, approval gate, never delete first.
3. **bridge.md** — .claude/agents/bridge.md. The router agent the Secretary delegates to.

## WHAT THE SYSTEM IS
A control system for handling code/text "patches" on the Batocera box. Paste/grab
content -> it gets read, judged, routed. Patcher = control panel, NOT engine.

## PIPELINE (LOCKED)
SCAN -> EXTRACT -> CLASSIFY -> COMPARE -> DECIDE -> ROUTE -> VERIFY -> LOG

## PARTS THAT EXIST
Librarian (scanner/header parser), Code Labeler + Preflight Bridge (judges content,
lamps GREEN/ORANGE/RED/BLUE), HeadHunter (header truth engine), CodeBank (extract/store),
Patcher GUI (umpatcher.py — control panel), Main Menu (um.py), HAMMER (action layer),
heavy append-only logs/thinktank.

## CORE PROBLEM (recurring, from all handoffs)
**Overbuilt and underconnected.** All parts exist; no orchestrator (CARB) ties them
into one flow. Modules run alone. Past damage came from REBUILDING instead of
STITCHING (broken menus, indentation errors, monolith that blew the buffer, a rescue
that lost the GUI). ORDER: stitch what exists, do not build new programs.

## LOCKED RULES
Read the Brain first; ask God; assume nothing. No blind rewrites. No multi-branch
changes. No "should work" claims. Proof on-screen. Append-only logs. One socket = one
file. Stop building new pieces — stitch the existing system.

## KNOWN GAPS / OPEN ITEMS
- brain_ticket.txt referenced by CLAUDE.md is MISSING from backup. Only existing
  Brain ticket/state file: handoffs/2026-04-14/SESSION_CHAT_BRAIN_TICKET_AND_STATE.txt
- ultramode_aibridge.py source not in backup (fingerprint only).
- CARB orchestrator: still MISSING (the central decide->call-next-step spine).
- Full CC roster incomplete — many live CCs not yet recorded by name.

## SYSTEM STATE
Engine: READY | UI: PARTIAL | Logs: HEAVY | Orchestration: MISSING

EOL ###
